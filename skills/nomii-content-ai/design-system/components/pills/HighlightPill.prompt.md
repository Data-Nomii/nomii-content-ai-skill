One-line: The filled celeste capsule that carries NOMII's stats, hooks and empathetic gains — always white Rubik Medium Italic.

```jsx
<HighlightPill>4 de cada 5 médicos</HighlightPill>
<HighlightPill tone="azul" size="lg">Contrato garantizado</HighlightPill>
```

Variants: `tone` celeste (default) | azul. `size` sm | md | lg. Text is always italic — this is the one place italic is mandatory. Pair with running Light copy around it.
